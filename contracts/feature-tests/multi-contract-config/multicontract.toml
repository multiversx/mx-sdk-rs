[settings]
default = "main"

[contract.main]

[contract.custom]
external-view = "true"
wasm-name = "multisig-ev.wasm"

[labels.main]
contracts = ["main", "custom"]

[labels.custom]
contracts = ["custom"]

name: CI

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  contracts:
    name: Contracts
    uses: ElrondNetwork/elrond-actions/.github/workflows/contracts.yml@contract-actions
    with:
      extra-build-args: "--ignore-eei-checks"
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}

name: Build and upload multisig

on:
  pull_request:

permissions:
  contents: write

jobs:
  build:
    uses: multiversx/mx-sc-actions/.github/workflows/reproducible-build.yml@3467a9a0596d86011e11c910fce6af1a7a0a975b
    with:
      image_tag: v4.1.3
      attach_to_existing_release: false
      project_path: ./contracts/examples
      contract_name: multisig
